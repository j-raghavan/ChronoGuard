# ChronoGuard Environment Configuration
# =====================================
# This file contains the MINIMUM required environment variables.
# Copy to `.env` and fill in your values.

# -----------------------------------------------------------------------------
# PRODUCTION ESSENTIALS (Required)
# -----------------------------------------------------------------------------

# Database Password
# Strong password for PostgreSQL (used by TimescaleDB)
CHRONOGUARD_DB_PASSWORD=CHANGEME_strong_db_password_here_32chars

# JWT Secret Key
# Cryptographically random key for JWT token signing (min 32 chars)
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
CHRONOGUARD_SECURITY_SECRET_KEY=CHANGEME_random_secret_key_here_43chars_minimum

# OPA→API Internal Authentication
# Shared secret for OPA policy engine to authenticate with ChronoGuard API
CHRONOGUARD_INTERNAL_SECRET=CHANGEME_opa_api_shared_secret_32chars

# -----------------------------------------------------------------------------
# DEMO MODE (Optional - Development/Demo Only)
# -----------------------------------------------------------------------------
# Uncomment to enable simplified auth for demos/development
# WARNING: Never enable in production!

# CHRONOGUARD_SECURITY_DEMO_MODE_ENABLED=true
# CHRONOGUARD_SECURITY_DEMO_ADMIN_PASSWORD=CHANGEME_demo_password_minimum_16_characters

# -----------------------------------------------------------------------------
# ADVANCED (Optional - Override Defaults Only If Needed)
# -----------------------------------------------------------------------------
# All settings below have sensible defaults. Only uncomment if you need custom values.

# Database Connection (defaults: localhost:5432, user=chronoguard, db=chronoguard)
# CHRONOGUARD_DB_HOST=localhost
# CHRONOGUARD_DB_PORT=5432
# CHRONOGUARD_DB_USER=chronoguard
# CHRONOGUARD_DB_DATABASE=chronoguard

# Redis (defaults: localhost:6379, db=0)
# CHRONOGUARD_REDIS_HOST=localhost
# CHRONOGUARD_REDIS_PORT=6379
# CHRONOGUARD_REDIS_PASSWORD=

# Session Cookies (defaults: secure=true, samesite=lax, httponly=true)
# CHRONOGUARD_SECURITY_SESSION_COOKIE_SECURE=true
# CHRONOGUARD_SECURITY_SESSION_COOKIE_SAME_SITE=lax

# API Server (defaults: 127.0.0.1:8000)
# CHRONOGUARD_API_HOST=127.0.0.1
# CHRONOGUARD_API_PORT=8000

# Observability (optional)
# JAEGER_AGENT_HOST=jaeger
# JAEGER_AGENT_PORT=6831
